name: test
channels:

  - conda-forge
  - acellera

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - pytorch
  - moleculekit

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

